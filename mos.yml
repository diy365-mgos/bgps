author: DIY365
type: lib
description: This Mongoose OS library is the base for implementing GPS libraries.
version: 1.0

sources:
  - src

includes:
  - include

filesystem:
  - fs

config_schema:
  - ["gps", "o", {title: "GPS config"}]
  - ["gps.update_interval", "i", 5000, {"title": "Update interval in milliseconds."}]

libs:

tags:
  - c

manifest_version: 2017-09-29
