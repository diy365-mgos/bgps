author: DIY365
type: lib
description: This Mongoose OS library is the base for implementing GPS libraries.
version: 1.0

sources:
  - src

includes:
  - include

filesystem:
  - fs

config_schema:
  - ["gps", "o", {title: "GPS config"}]
  - ["gps.auto_update", "o", {title: "GPS auto-update config"}]
  - ["gps.auto_update.enable", "b", true, {title: "Enable the auto-update"}]
  - ["gps.auto_update.interval", "i", 5000, {title: "Auto-update interval (in milliseconds)"}]

libs:

tags:
  - c

manifest_version: 2017-09-29
